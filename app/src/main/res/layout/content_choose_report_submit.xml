<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/chooseReportSubmitTitle"
        tools:text="@string/submit_report"
        android:textAlignment="center"
        android:layout_margin="10dp"
        android:textSize="30sp" />

    <Button
        android:text="@string/submit_source_report"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonSourceReport"
        android:layout_below="@id/chooseReportSubmitTitle"
        android:layout_centerInParent="true"
        android:layout_marginTop="100dp"
        android:onClick="chooseSourceReport"/>

    <Button
        android:text="@string/submit_purity_report"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonPurityReport"
        android:layout_below="@id/buttonSourceReport"
        android:layout_marginTop="30dp"
        android:layout_centerInParent="true"
        android:onClick="choosePurityReport"/>
</RelativeLayout>